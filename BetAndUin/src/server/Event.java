package server;
/* A class that works only to wake up the ConnectionWithServerManager when some events happen.
 * Those events can be the arrival of a new message or instead, the need of sending a message
 * at a given time.
 * The complete list of events should be specified later.
 */
public class Event {
	public Event(){
		
	}
}
